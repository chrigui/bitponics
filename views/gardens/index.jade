extends ../layout

block head

block css

block content
	- if (typeof(userGrowPlanInstances) != 'undefined' && userGrowPlanInstances.length > 0)
		h2 My Gardens
		ul
		each gpi in userGrowPlanInstances
			li
				a(href="/gardens/#{gpi._id}")
					p Name: #{gpi.name} 
					p Id: #{gpi._id} 
					p Active: #{gpi.active}
					p Start Date: #{gpi.startDate}
					p Device: #{gpi.device}
				hr
	
	h2 Community Gardens
	p Coming soon...

	block footer-scripts
	script
		bpn.pageData.userGrowPlanInstances = !{JSON.stringify(userGrowPlanInstances)};
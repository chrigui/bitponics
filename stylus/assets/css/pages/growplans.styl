@import "../libs/twitter-bootstrap"
@import "../components/select2"
@import "../global/breakpoints"

// .landing-page.growplans
.growplans
	
	.plantlist
		margin-top .5em
		margin-bottom .5em
		padding 0

		&.float
			overflow hidden
			border 1px dashed color-border-gray
			li
				margin 0 auto auto
				float left

		&.small-float
			overflow hidden
			margin 0
			li
				font-size .75em
				float left

				.btn
					margin 0 0.5em 1em

					&.add
						border 1px solid color-logo-green


		.grid-cell
			// max-width 40em
			margin 0 auto
		
		.img-wrapper
			position relative
			height 15em
			overflow hidden
			max-width 40em
			margin 0 auto

			img
				position absolute
				top 0
				min-width 100%
				min-height 100%
				width auto
				max-width 135%

	.btn
		padding .5em

	.overlay 
		.btn
			margin 0

		.custom-checkbox-wrap
			bottom 4em
			text-align center
			.plant
				&:before
					top 1.1em
					right .85em

		// .img-wrapper
		// 	.icon-glyph.icon-glyphlogo-new
		// 		position absolute
		// 		top 0
		// 		left 0

	.edit-btn
		@extends .display-ib
		height 1em
		padding-left 1em
		position relative
		margin 0 0 1em

		i
			position absolute
			top 0
			left 0

	.next-step-btn, .phase-tabs .btn
		background white!important
		color color-logo-green!important
		border 3px solid color-logo-green
		padding .5em 2em
		display block
		text-align center
		width 15em

		.icon-glyph-new
			color color-logo-green

	.question
		.grid 
			ul
				// clear both
			li
				margin 0
				width auto
				.btn
					margin .7em 1em 1em 0
					width auto


		.add
			float left
			clear left
			margin .7em 1em 1em 0
			// margin-top 0	


	.img-wrapper
		position relative
		.icon-glyph
			height: 5em;
			width: 5em;
			background-position: -1em 0;

	.icon-glyph-new
		height: 1em;
		font-size: 1.25em;
		position: relative;
		top: 0.15em;

	// .grid .icon-glyphlogo-new
	// 	display block!important
	// 	position absolute
	// 	top 1em
	// 	left 1em

	// dl
	// 	font-size .85em
	// dt
	// 	font-size: 1.15em;
	// dd
	// 	margin 0 0 .25em
	// 	min-height 3em

	#growplans_activate
		@extends .display-ib
		clear both

	.spinner
		@extends .display-ib
		width: 2em;
		margin: 1em;

	.phaseDescription
		margin 0 0 2em 0
		
	.phase-tabs 
		list-style: none;
		// margin: 0;
		// padding: 0;
		clear: both;
		overflow: hidden;

		.btn
			width auto

		div
			padding-left: 1em;

		.selected
			background color-logo-green!important
			color white!important

			.icon-glyph-new
				color white!important

		li {
			position: relative;
			// width: 25%;
			width auto
			float: left;
			padding: .25em .5em;
			// color: white;
			//background: green;
			// cursor: pointer;
			// height: auto;
			//text-shadow: 1px 1px 1px rgba(0, 0, 0, .5);
			// font-weight: bold;
			margin: 1em .25em 1em 0;

			&.focused {
				text-decoration: underline;
			}

			&.inactive {
				display: block;
				opacity: 1;
				background-color: rgba(36, 211, 33, .6);
			}

			.toggle {
				text-decoration: none;
			}
		}


	.idealRanges, .actions {
		position: relative;
		clearfix()

		ul {
			list-style: none;
			margin: 0;
			padding: 0;
		}

		.question {
			margin: 0 1em 1em 0;
			border: 1px solid color-border-gray;
			padding: 2em 1em 1em; 
			position: relative;
		}

		.btn {
			margin-left 0
			margin-top 0

			&.close {
				position: absolute;
				top: 0;
				right: -.25em;
				cursor: pointer;
				background: #fff;
				color: color-text-gray;
			}

		}

		input[type=checkbox] {
			opacity: 1;
			height: auto;
			width: auto;
			margin: auto;
			display: inline;
		}

		.separator-top {
			margin-top: 1em;
		}

		.hi, .lo {

			label, textarea {
				display: block;
				width: 100%;
			}

			input[type=number] {
				@extends .display-ib;
				width: 6em;
				margin-right: 1em;
			}

			label {
				// font-size: 2em;
			}
		}

	}
	.phaseEndDescription
		margin 2em 0 2em
		clear both

	
	div[data-bpn-directives-navigation-phases-graph]
		width 60%

	.ring-graph
		.good
			fill: color-light-green
		.active
			fill: color-bg-green
		.add-phase
			fill: color-light-green
			opacity: .45

		text
			font-size 1em

	.controls
		.grid--wrapping > .grid-cell
			flex 1 5em
			font-size: .85em;
			padding: 4em 0 0;
			line-height: 1;
			font-weight: normal;
			margin: 0 0 1em;
			max-width: 5em;

	.grow-systems, .lights, .nutrients, .ideal-ranges
		.grid--wrapping > .grid-cell
			flex 1 50%
			font-size: .85em;
			padding: 0;
			line-height: 1;
			font-weight: normal;
			margin: 0 0 1em;
			max-width: 50%;

			.btn
				margin 0 1em 1em 0

	.side
		transition opacity .5s linear
	/*
	 * Split layout Overrides
	 */
	// 

	.grow-plan-customize
		h2.page-heading
			@extends .float-l
			max-width 49%
			clear left

			.garden-name
				position relative
				



		ul.plantlist
			@extends .float-l
			max-width 80%
			clear left
		
		header 
			@extends .position-r
			input
				@extends .position-a
				bottom: -.5em;
				right: 0;
				font-size 1em

		h2.page-heading [contenteditable], btn[contenteditable]
			outline none
			border-bottom 1px dashed color-logo-green
			white-space nowrap
			font-style italic

		.seamless-input
			background: color-logo-green
			color: white;
			border: none;

		#phases
			.page-heading-small
				@extends .text-align-c
				float none
			.circle
				margin-top 0
				svg
					max-width 100%
	

/*
 * Animations
 */
.accordion {
  -webkit-transition:0.25s linear all;
  transition:0.25s linear all;
  opacity:1;
}

.accordion.ng-hide {
  opacity:0;
  height: 0;
}

.accordion.ng-hide-add, .accordion.ng-hide-remove {
  /* this needs to be here to make it visible during the animation
     since the .ng-hide class is already on the element rendering
     it as hidden. */
  display:block!important;
}

.grid-cell-animation.ng-enter, 
.grid-cell-animation.ng-leave, 
.grid-cell-animation.ng-move {
  -webkit-transition: 0.5s linear all;
  transition: 0.5s linear all;
  position:relative;
}

.grid-cell-animation.ng-enter {
  top:-10px;
  opacity:0;
}
.grid-cell-animation.ng-enter.ng-enter-active {
  top:0;
  opacity:1;
}

.grid-cell-animation.ng-leave {
  top:0;
  opacity:0;
}
.grid-cell-animation.ng-leave.ng-leave-active {
  // top:-10px;
  // opacity:0;
}
.grid-cell-animation.ng-move {
  // opacity:0.5;
}
.grid-cell-animation.ng-move.ng-move-active {
  // opacity:1;
}